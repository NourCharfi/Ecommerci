@model dynamic

@if (Model != null)
{
    <div class="alert alert-@(Model.BadgeClass.Replace("badge-", "")) mb-2" role="alert">
        <i class="fas fa-hourglass-end me-2"></i>
        <strong>@Model.TimeText</strong>
    </div>
}
